language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t psinalberth/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true psinalberth/docker-react npm run test -- --coverage